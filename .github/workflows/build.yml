name: akaLink Compact build

on:
  push:
    branches: [ "**" ]
  pull_request:
    branches: [ "**" ]

  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install WCH modified GCC toolchain
        run: |
          cd ~
          git clone https://github.com/akkako/riscv-wch-elf-gcc12.git
          cd ./riscv-wch-elf-gcc12
          tar -Jxf MRS_Toolchain_Linux_x64_V230.tar.xz
          cd Toolchain
          mv "RISC-V Embedded GCC12" "gcc12"

      - name: Build Bootloader
        run: |
          cd firmware/Bootloader
          export PATH=$PATH:~/riscv-wch-elf-gcc12/Toolchain/gcc12/bin
          make all -j

      - name: Build Application
        run: |
          cd firmware/Application
          export PATH=$PATH:~/riscv-wch-elf-gcc12/Toolchain/gcc12/bin
          make all -j

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: build-artifact
          path: |
#            firmware/Application/build_exec/akaLink_Compact_App.bin
            firmware/Application/build_exec/akaLink_Compact_App_pack.bin
#            firmware/Application/build_exec/akaLink_Compact_App.elf
#            firmware/Application/build_exec/akaLink_Compact_App.hex
#            firmware/Bootloader/build_exec/akaLink_Compact_Dfu.bin
            firmware/Bootloader/build_exec/akaLink_Compact_Dfu_pack.bin
#            firmware/Bootloader/build_exec/akaLink_Compact_Dfu.elf
#            firmware/Bootloader/build_exec/akaLink_Compact_Dfu.hex
      
  release:
    runs-on: ubuntu-latest
    needs: build
    if: startsWith(github.ref, 'refs/tags/')
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true

      - name: Download release
        uses: actions/download-artifact@v4
        with:
          name: build-artifact
          path: releases

      - name: Create Release
        uses: ncipollo/release-action@v1
        with:
          artifacts: 'releases/*'
          allowUpdates: true
          generateReleaseNotes: true